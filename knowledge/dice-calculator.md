# Dice Tray v3.7.0 — Bandeja de Dados

## API

```js
// Sin global; clases DiceTrayPopOut, DiceCreator, DiceRowSettings, *DiceMap (por sistema)
```

## Hooks

- `activateChatLog`, `chatMessage`, `renderChatLog`, `getSceneControlButtons`
- `dice-calculator.forceRender`, `dice-calculator.keymaps`

## Uso

- Bandeja de dados (Dice Tray) en el chat para lanzar dados por sistema (dnd5e, pf2e, swade, etc.). Hooks propios `dice-calculator.*` para forzar render y keymaps.

## Dependencias

- Ninguna (standalone, multi-sistema).
